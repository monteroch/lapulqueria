import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  eventItemContainer: {
    borderColor: "#000000",
    border: "2px solid",
    height: "50%",
    margin: "10px"
  }
}));
