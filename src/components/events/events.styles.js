import { makeStyles } from '@material-ui/core/styles';
export default makeStyles((theme) => ({
    eventsContainer: {
        borderColor: "#ff033e",
        border: "5px solid",
        height: "100%",
        boxSizing: "border-box",
        padding: "5%"
    }
}));