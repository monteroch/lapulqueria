import { makeStyles } from '@material-ui/core/styles';
export default makeStyles((theme) => ({
    root: {
        flexGrow: 1
    },

    eventsContainer: {
        borderColor: "#ff033e",
        height: "500px",
        paddingTop: "50px"
    },

    itemGrid: {
        backgroundColor: "#915c83",
        border: '2px solid',
        borderColor: '#000000'
    }
}));